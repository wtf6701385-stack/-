-- [[ 🐾 影子貓貓 2026：終極主腳本 ]] --

-- 1. 加載 UI 庫 (Ok)
local NekoLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/Ok"))()
local win = NekoLib.new("NEKO SYSTEM")

-- 2. 預先宣告全局變數 (這是對接 JK 的關鍵！)
_G.Settings = {
    Main = {
        ["Auto Farm"] = false,
        ["Select Weapon"] = "Melee",
        ["Auto Fast Farm"] = false
    },
    Setting = {
        ["Fast Attack"] = true,
        ["Auto Haki"] = true,
        ["Attack Aura"] = false
    }
}

-- 3. UI 界面構建
local MainTab = win:Tab("主功能頁")
local FarmSec = MainTab:Section("自動農場")

FarmSec:Toggle("全自動刷等級喵", function(s)
    _G.Settings.Main["Auto Farm"] = s
end)

local CombatSec = MainTab:Section("戰鬥強化")
CombatSec:Toggle("無冷卻快速攻擊", function(s)
    _G.Settings.Setting["Fast Attack"] = s
end)

-- 4. 武器切換
local SetSec = MainTab:Section("設定")
SetSec:Button("切換為近戰武器", function() _G.Settings.Main["Select Weapon"] = "Melee" end)
SetSec:Button("切換為劍類武器", function() _G.Settings.Main["Select Weapon"] = "Sword" end)

-- 5. 最後啟動大腦 (JK)
task.spawn(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/JK"))()
end)

print("🐾 貓貓系統啟動成功喵！")
