-- [[ 🐾 貓貓核心功能 (NEKOOOOO) - 2026 戰鬥邏輯模組 🐾 ]] --
-- 保持原有接口：_G.NekoRunning, _G.NekoAttack, _G.FruitAttack, _G.Settings

local player = game:GetService("Players").LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local Camera = workspace.CurrentCamera

-- 🛠️ 強化型：確保隨時抓到最新的 Character
local function GetChar()
    return player.Character or player.CharacterAdded:Wait()
end

-- 🛠️ 強化型：自動尋找 Net 路徑 (兼容一、二、三海)
local function GetNet()
    return ReplicatedStorage:FindFirstChild("Net", true) or 
           (ReplicatedStorage:FindFirstChild("Modules") and ReplicatedStorage.Modules:FindFirstChild("Net"))
end

-- ✨ [視野與鏡頭修正]
task.spawn(function()
    while true do
        if _G.NekoRunning then
            pcall(function()
                if Camera and _G.Settings then
                    Camera.FieldOfView = _G.Settings.FOV
                    player.CameraMaxZoomDistance = _G.Settings.ZoomDist
                end
            end)
        end
        task.wait(0.5)
    end
end)

-- ✨ [絲滑加速]
RunService.Heartbeat:Connect(function()
    if not _G.NekoRunning or not _G.Settings or not _G.Settings.SpeedEnabled then return end
    local char = GetChar()
    if char and char:FindFirstChild("Humanoid") then
        local hum = char.Humanoid
        if hum.MoveDirection.Magnitude > 0 then
            char:TranslateBy(hum.MoveDirection * (_G.Settings.CatSpeedValue / 10))
        end
    end
end)

-- ✨ [戰鬥循環 A：神刀]
task.spawn(function()
    while true do
        if _G.NekoRunning and _G.NekoAttack and _G.Settings and _G.Settings.Mode ~= "None" then
            pcall(function()
                local char = GetChar()
                local root = char:FindFirstChild("HumanoidRootPart")
                local Net = GetNet()
                
                if not (root and Net) then return end

                local NekoTargets, NekoPart = {}, nil
                
                local function Scan(folder, isPlayers)
                    if not folder then return end
                    for _, obj in ipairs(folder:GetChildren()) do
                        local enemyChar = isPlayers and (obj.Character or obj) or obj
                        if enemyChar and enemyChar ~= char then
                            local eH = enemyChar:FindFirstChildOfClass("Humanoid")
                            local eR = enemyChar:FindFirstChild("HumanoidRootPart")
                            
                            if eH and eH.Health > 0 and eR then
                                local dist = (eR.Position - root.Position).Magnitude
                                if dist < (_G.Settings.Distance or 1500) then
                                    if _G.Settings.PullEnemies and not isPlayers then 
                                        eR.CFrame = root.CFrame * CFrame.new(0, 0, -3) 
                                    end
                                    NekoPart = eR
                                    table.insert(NekoTargets, {enemyChar, eR})
                                end
                            end
                        end
                    end
                end

                Scan(workspace:FindFirstChild("Enemies") or workspace, false)
                if _G.Settings.Mode == "All" then Scan(game:GetService("Players"), true) end
                
                if NekoPart and #NekoTargets > 0 then 
                    -- 使用 Safe Invoke 確保 Remote 存在
                    local regAtk = Net:FindFirstChild("RE/RegisterAttack")
                    local regHit = Net:FindFirstChild("RE/RegisterHit")
                    if regAtk then regAtk:FireServer(_G.Settings.ClickDelay or 0.1) end
                    if regHit then regHit:FireServer(NekoPart, NekoTargets) end
                end
            end)
        end
        task.wait(_G.Settings and _G.Settings.ClickDelay or 0.1)
    end
end)

-- ✨ [戰鬥循環 B：果實] (同理修復路徑)
task.spawn(function()
    while true do
        if _G.NekoRunning and _G.FruitAttack and _G.Settings then
            pcall(function()
                local char = GetChar()
                local root = char:FindFirstChild("HumanoidRootPart")
                local tool = char:FindFirstChildOfClass("Tool")
                local Net = GetNet()
                
                if root and tool and Net then
                    local target, dist = nil, _G.Settings.Distance
                    local enemies = workspace:FindFirstChild("Enemies") or workspace
                    
                    for _, v in ipairs(enemies:GetChildren()) do
                        local eH, eR = v:FindFirstChildOfClass("Humanoid"), v:FindFirstChild("HumanoidRootPart")
                        if eH and eH.Health > 0 and eR and v ~= char then
                            local m = (eR.Position - root.Position).Magnitude
                            if m < dist then dist = m; target = eR end
                        end
                    end
                    
                    if target then
                        root.CFrame = CFrame.new((target.CFrame * CFrame.new(0, _G.Settings.FruitHeight, -3)).p, target.Position)
                        local regAtk = Net:FindFirstChild("RE/RegisterAttack")
                        local regHit = Net:FindFirstChild("RE/RegisterHit")
                        if regAtk then regAtk:FireServer() end
                        if regHit then regHit:FireServer(target, {{target.Parent, target}}) end
                        tool:Activate()
                    end
                end
            end)
        end
        task.wait(_G.Settings and _G.Settings.FruitDelay or 0.5)
    end
end)

-- ✨ [自動 V4]
task.spawn(function()
    while true do
        if _G.NekoRunning and _G.AutoV4 then
            pcall(function()
                local char = GetChar()
                local awake = char:FindFirstChild("Awakening") or player.Backpack:FindFirstChild("Awakening")
                if awake and awake:FindFirstChild("RemoteFunction") then 
                    awake.RemoteFunction:InvokeServer(true) 
                end
            end)
        end
        task.wait(1)
    end
end)

print("🐾 NEKOOOOO 核心已修復路徑與角色判定，現在非常穩定喵！")
