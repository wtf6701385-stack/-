-- [[ 🐾 影子貓貓 2026 終極合體啟動器 🐾 ]]

-- 1. 加載主人剛給我的 UI 庫
local NekoLib = loadstring(game:HttpGet("妳的GitHub_UI庫連結"))()
local win = NekoLib.new("NEKO SYSTEM") -- 這裡會觸發那個超帥的 INITIALIZING 動畫

-- 2. 預先宣告 _G.Settings 表格 (確保 NEKO 檔案讀得到)
-- 影子檢查過 NEKO 檔案，這些是它最重要的核心開關
_G.Settings = {
    Main = {
        ["Auto Farm"] = false,
        ["Select Weapon"] = "Melee", -- 預設近戰
        ["Auto Fast Farm"] = false,
        ["Farm Level Method"] = "Quest"
    },
    Setting = {
        ["Fast Attack"] = true,
        ["Auto Haki"] = true,
        ["Attack Aura"] = false,
        ["Bring Mob"] = true
    },
    Stats = {
        ["Auto Add Melee Stats"] = false,
        ["Auto Add Defense Stats"] = false
    }
}

-- 3. 開始對接 UI 分頁與功能
local MainTab = win:Tab("主功能頁")
local FarmSec = MainTab:Section("自動農場")

-- 對接「自動刷等級」
FarmSec:Toggle("全自動刷等級喵", function(s)
    _G.Settings.Main["Auto Farm"] = s
end)

-- 對接「極速刷怪」
FarmSec:Toggle("極速刷怪 (Fast Farm)", function(s)
    _G.Settings.Main["Auto Fast Farm"] = s
end)

local CombatSec = MainTab:Section("戰鬥強化")

-- 對接「快速攻擊」
CombatSec:Toggle("無冷卻快速攻擊", function(s)
    _G.Settings.Setting["Fast Attack"] = s
end)

-- 對接「攻擊光環」
CombatSec:Toggle("全屏攻擊光環", function(s)
    _G.Settings.Setting["Attack Aura"] = s
end)

-- 4. 武器選擇按鈕 (示範如何用 Button 對接)
local SetSec = MainTab:Section("設定")
SetSec:Button("切換為近戰武器", function()
    _G.Settings.Main["Select Weapon"] = "Melee"
    game:GetService("StarterGui"):SetCore("SendNotification", {Title = "貓貓通知", Text = "已切換為近戰！"})
end)

SetSec:Button("切換為劍類武器", function()
    _G.Settings.Main["Select Weapon"] = "Sword"
    game:GetService("StarterGui"):SetCore("SendNotification", {Title = "貓貓通知", Text = "已切換為劍！"})
end)

-- 5. 最後一步：召喚那 5000 行的「凌霄大腦」
-- 影子提醒：因為 NEKO 檔案裡原本就有 WindUI，
-- 我們讓它跑在後台，它會發現 _G.Settings 被我們改了，就會自動動起來！
loadstring(game:HttpGet("妳的_NEKO_5000行代碼連結"))()

print("🐾 系統已完美對接：貓貓 UI 已接管凌霄大腦！")
