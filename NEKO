-- // [ 貓貓腳本 | 核心加載器 ] // --
-- // 戰鬥模組：已注入影剝離的鎖頭核心 // --

local Players = game:GetService("Players")
local StarterGui = game:GetService("StarterGui")
local RunService = game:GetService("RunService")
local localPlayer = Players.LocalPlayer
local Camera = game:GetService("Workspace").CurrentCamera

-- // [ 第一層：授權資料庫 ] // --
local Whitelist = {
    ["Zn6xx"] = true,
    ["jay04_23"] = true,
    ["iandsere"] = true,
    ["Kadys5373"] = true,
}

-- // [ 第二層：認證邏輯處理 ] // --
local function Authenticate()
    local name = localPlayer.Name
    
    if Whitelist[name] then
        StarterGui:SetCore("SendNotification", {
            Title = " 貓貓腳本 啟動成功 ",
            Text = " 貓貓好想你！！！ ",
            Duration = 5
        })

        local success, NEKOLib = pcall(function()
            return loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOUI?t="..tick()))()
        end)
        if not success or not NEKOLib then return end

        local Lib = NEKOLib:Init("ฅ^•ﻌ•^ฅ")

        -- // 環境狀態
        _G.PVP = false
        _G.MOB = false
        _G.AFK = false
        _G.Aimbot = false -- 影：鎖頭開關
        local Loaded = { PVP = false, MOB = false }

        -- // UI 頁面：貓公告
        local Tab0 = Lib:CreateTab("貓公告", true) 
        Tab0:AddInfo("貓貓公告", "貓貓一個人做的，如果有問題馬上說")

        -- // UI 頁面：貓戰鬥 (強化版)
        local Tab1 = Lib:CreateTab("貓戰鬥", false)
        local btnPVP, btnMOB, btnAim

        btnPVP = Tab1:AddButton("打架模式 : OFF", function(self)
            _G.PVP = not _G.PVP
            if _G.PVP then
                _G.MOB = false
                if btnMOB then btnMOB.Text = "  🐱 刷怪模式 : OFF" end
                if not Loaded.PVP then
                    Loaded.PVP = true
                    task.spawn(function() pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOPVP?t="..tick()))() end) end)
                end
            end
            self.Text = "  🐱 打架模式 : " .. (_G.PVP and "ON" or "OFF")
        end)

        btnMOB = Tab1:AddButton("刷怪模式 : OFF", function(self)
            _G.MOB = not _G.MOB
            if _G.MOB then
                _G.PVP = false
                if btnPVP then btnPVP.Text = "  🐱 打架模式 : OFF" end
                if not Loaded.MOB then
                    Loaded.MOB = true
                    task.spawn(function() pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOMOB?t="..tick()))() end) end)
                end
            end
            self.Text = "  🐱 刷怪模式 : " .. (_G.MOB and "ON" or "OFF")
        end)

        -- 影：注入的鎖頭按鈕，放在刷怪模式下面
        btnAim = Tab1:AddButton("貓鎖頭 : OFF", function(self)
            _G.Aimbot = not _G.Aimbot
            self.Text = "  🐱 貓鎖頭 : " .. (_G.Aimbot and "ON" or "OFF")
        end)

        -- // UI 頁面：貓功能
        local Tab2 = Lib:CreateTab("貓功能", false)
        Tab2:AddButton("防掛機模式 : OFF", function(self)
            _G.AFK = not _G.AFK
            self.Text = "  🐱 防掛機模式 : " .. (_G.AFK and "ON" or "OFF")
        end)

        -- // 鎖頭與防掉線守護進程 // --
        task.spawn(function()
            local VirtualUser = game:GetService("VirtualUser")
            
            -- 鎖頭搜尋邏輯 (影)
            local function GetTarget()
                local maxDist = 3000
                local target = nil
                -- 優先搜尋 Enemies 資料夾 (BF 特色)
                local enemyFolder = workspace:FindFirstChild("Enemies") or workspace
                for _, v in pairs(enemyFolder:GetChildren()) do
                    if v:FindFirstChild("HumanoidRootPart") and v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 then
                        local dist = (localPlayer.Character.HumanoidRootPart.Position - v.HumanoidRootPart.Position).Magnitude
                        if dist < maxDist then
                            target = v
                            maxDist = dist
                        end
                    end
                end
                return target
            end

            RunService.RenderStepped:Connect(function()
                -- 防掉線
                if _G.AFK then
                    VirtualUser:CaptureController()
                    VirtualUser:ClickButton2(Vector2.new())
                end
                -- 鎖頭執行
                if _G.Aimbot and (_G.MOB or _G.PVP) then
                    local t = GetTarget()
                    if t and t:FindFirstChild("Head") then
                        Camera.CFrame = CFrame.new(Camera.CFrame.Position, t.Head.Position)
                    end
                end
            end)
        end)
        
    else
        -- 授權失敗邏輯... (略)
    end
end

Authenticate()
