-- [[ GitHub: NEKO - Original Logic Kept ]]
local success, NEKOLib = pcall(function()
    return loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOUI"))()
end)

if not success or not NEKOLib then
    warn("❌ NEKO UI 載入失敗！")
    return
end

local Lib = NEKOLib:Init()
local player = game:GetService("Players").LocalPlayer

-- [[ 原始全域變數 ]]
_G.PVP = false
_G.MOB = false
_G.FastAttack = false
_G.AutoV4 = false 

-- [[ 核心邏輯：保持視角鎖定 + 原有的 V4 檢查 ]]
game:GetService("RunService").Heartbeat:Connect(function()
    -- 內建自動視角 (1000)
    pcall(function()
        player.CameraMaxZoomDistance = 1000
    end)

    -- 原始 AutoV4 邏輯
    if _G.AutoV4 then
        pcall(function()
            local bp = player:FindFirstChild("Backpack")
            local char = player.Character
            local awake = (bp and bp:FindFirstChild("Awakening")) or (char and char:FindFirstChild("Awakening"))
            if awake then 
                awake.RemoteFunction:InvokeServer(true) 
            end
        end)
    end
end)

local function KillCombat()
    _G.FastAttack = false
    _G.CurrentFastAtkID = "KILL_" .. tick()
end

-- 以下為你原本的所有 UI 按鈕與功能邏輯，完全沒變動 --
local p0, l0 = Lib:CreateTab("貓公告", true)
Lib:AddInfo(p0, l0, "🐾 狀態", "UI 已成功載入")

local p1, l1 = Lib:CreateTab("貓戰鬥")
local btnPVP, btnMOB

btnPVP = Lib:AddButton(p1, l1, "打架模式 : OFF", function(self)
    _G.PVP = not _G.PVP
    if _G.PVP then
        _G.MOB = false
        if btnMOB then btnMOB.Text = "  🐱 刷怪模式 : OFF" end
        KillCombat()
        task.wait(0.1)
        _G.FastAttack = true
        loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOPVP"))()
    else
        KillCombat()
    end
    self.Text = "  🐱 打架模式 : " .. (_G.PVP and "ON" or "OFF")
end)

btnMOB = Lib:AddButton(p1, l1, "刷怪模式 : OFF", function(self)
    _G.MOB = not _G.MOB
    if _G.MOB then
        _G.PVP = false
        if btnPVP then btnPVP.Text = "  🐱 打架模式 : OFF" end
        KillCombat()
        task.wait(0.1)
        _G.FastAttack = true
        loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOMOB"))()
    else
        KillCombat()
    end
    self.Text = "  🐱 刷怪模式 : " .. (_G.MOB and "ON" or "OFF")
end)

local p2, l2 = Lib:CreateTab("貓功能")
Lib:AddButton(p2, l2, "貓貓神速", function() loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOSPD"))() end)
Lib:AddButton(p2, l2, "貓貓透視", function() loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOESP"))() end)

Lib:AddButton(p2, l2, "自動 V4 : OFF", function(self)
    _G.AutoV4 = not _G.AutoV4
    self.Text = "  🐱 自動 V4 : " .. (_G.AutoV4 and "ON" or "OFF")
end)

local p3, l3 = Lib:CreateTab("貓召喚")
Lib:AddButton(p3, l3, "召喚飛行", function() loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOFly"))() end)
