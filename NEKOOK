local Players = game:GetService("Players")
local player = Players.LocalPlayer

-- 1. 從 GitHub 抓取白名單清單
local success, result = pcall(function()
    return game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/FUCKYOU")
end)

if success then
    local whitelist = loadstring(result)()
    
    -- 2. 檢查目前玩家的 UserId 是否在清單中
    if table.find(whitelist, player.UserId) then
        print("✅ 身份驗證成功，正在載入專屬 UI...")
        
        ------------------------------------------------
        -- 這裡放入你原本建立 UI 的代碼，或者是載入主腳本
        ------------------------------------------------
        -- 範例：載入你的 NEKO 腳本 (裡面包含 UI)
        loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKO"))()
        
    else
        -- 3. 不在白名單內的處理
        warn("❌ 存取被拒絕：你不在白名單中。")
        -- 這裡什麼都不做，所以對方不會看到任何 UI
    end
else
    warn("🌐 網路錯誤：無法連接至驗證伺服器。")
end
