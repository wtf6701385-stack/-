local Players = game:GetService("Players")
local player = Players.LocalPlayer
local userId = player.UserId

-- [[ 這裡只放純網址 ]]
local WHITELIST_URL = "https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/FUCKYOU"
local MAIN_SCRIPT_URL = "https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKO"

print("📡 正在驗證白名單...")

-- 1. 抓取白名單內容
local success, result = pcall(function()
    return game:HttpGet(WHITELIST_URL)
end)

if success then
    -- 2. 解析白名單
    local whitelistFunc = loadstring(result)
    if whitelistFunc then
        local whitelist = whitelistFunc()
        
        -- 3. 比對 UserId
        if table.find(whitelist, userId) then
            print("✅ 驗證成功，正在載入專屬 UI...")
            
            -- 載入主腳本
            local mainSuccess, err = pcall(function()
                loadstring(game:HttpGet(MAIN_SCRIPT_URL))()
            end)
            
            if not mainSuccess then
                warn("❌ 主腳本載入出錯: " .. tostring(err))
            end
        else
            warn("⛔ 您不在白名單清單中 (您的 ID: " .. userId .. ")")
        end
    else
        warn("❌ 白名單檔案內容格式錯誤")
    end
else
    warn("🌐 無法連線至 GitHub，請檢查連結是否有效。")
end
