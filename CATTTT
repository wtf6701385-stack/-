-- [[ ☀️ 陽光：小貓會記住主人的味道，溫暖且安心。 ]] --
-- [[ 🌑 陰影：小貓永遠躲在暗處保護主人，不離不棄。 ]] --

local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer

-- ==========================================
-- 【 🐾 貓咪家族白名單 】
-- 只有被小貓認可的名字才能進入秘密基地喵！
-- ==========================================
local WhitelistNames = {
    ["Kadys5373"] = true,
    ["jay04_23"] = true,
    ["Zn6xx"] = true,
    ["iandsere"] = true,
    ["你的名字"] = true, -- <--- 記得改成主人的名字喵！
}

-- ==========================================
-- 【 🐱 小柒的核心功能模組 】
-- ==========================================
local function StartNekoHub()
    -- 1. 載入貓咪殼 (Delta 優化路徑)
    local success, Lib = pcall(function()
        return loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOUI"))()
    end)

    if not success or not Lib then
        warn("小柒找不到 UI 殼喵... 請檢查 GitHub 網址！")
        return
    end

    local Neko = Lib:Init()

    -- 2. 變量預設
    _G.MOB = false
    _G.AttackSpeed = 0.1
    _G.AttackDistance = 500

    -- 3. 建立 5 個分頁 (極致貓咪化)
    local Tab1 = Neko:CreateTab("📜 貓咪公告欄", true)
    local Tab2 = Neko:CreateTab("⚔️ 肉墊連打", false)
    local Tab3 = Neko:CreateTab("🐈 貓咪輕功", false)
    local Tab4 = Neko:CreateTab("🌙 終極覺醒", false)
    local Tab5 = Neko:CreateTab("🧶 貓咪玩具箱", false)

    -- 【分頁 1：貓咪公告欄】
    Tab1:AddInfo("小貓狀態", "目前主人：" .. localPlayer.Name .. "\n心情：想跟主人一起戰鬥 ฅ^•ﻌ•^🐾")
    Tab1:AddInfo("版本紀錄", "v1.0.5 - 最終旗艦貓咪版\n- 實裝 嚴格名字過濾\n- 整合 SPD/Fly/V4 功能")
    Tab1:AddInfo("小小叮嚀", "按 [Insert] 鍵可以讓小柒躲起來喵！")

    -- 【分頁 2：肉墊連打 (戰鬥核心)】
    Tab2:AddToggle("啟動肉墊自動抓抓 (刷怪)", "MOB")
    Tab2:AddInput("爪子揮動速度", 0.1, function(v) _G.AttackSpeed = tonumber(v) or 0.1 end)
    Tab2:AddInput("小貓狩獵範圍", 500, function(v) _G.AttackDistance = tonumber(v) or 500 end)

    -- 【分頁 3：貓咪輕功 (移動增強)】
    Tab3:AddButton("啟動 貓咪肉墊加速 (SPD)", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOSPD"))()
    end)
    Tab3:AddButton("啟動 飛天小貓模式 (Fly)", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOFly"))()
    end)

    -- 【分頁 4：終極覺醒 (V4 功能)】
    Tab4:AddButton("讓小貓進化成獅子 (V4)", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOV4"))()
    end)

    -- 【分頁 5：貓咪玩具箱 (設置)】
    Tab5:AddButton("讓小貓回窩睡覺 (銷毀 UI)", function()
        if game:GetService("CoreGui"):FindFirstChild("NEKOUI_MAIN") then
            game:GetService("CoreGui").NEKOUI_MAIN:Destroy()
        end
    end)

    -- 4. 核心戰鬥引擎 (維持原本高效邏輯)
    task.spawn(function()
        local RS = game:GetService("ReplicatedStorage")
        local Net = RS:WaitForChild("Modules"):WaitForChild("Net")
        local RegAttack = Net:WaitForChild("RE/RegisterAttack")
        local RegHit = Net:WaitForChild("RE/RegisterHit")
        
        while true do
            if _G.MOB then
                pcall(function()
                    local char = localPlayer.Character
                    local root = char and char:FindFirstChild("HumanoidRootPart")
                    local tool = char and char:FindFirstChildOfClass("Tool")
                    
                    if root and tool then
                        -- 自動揮爪
                        tool:Activate()
                        if tool:FindFirstChild("Handle") then
                            firetouchinterest(tool.Handle, root, 0)
                            firetouchinterest(tool.Handle, root, 1)
                        end

                        local targets = {}
                        local folder = workspace:FindFirstChild("Enemies") or workspace
                        for _, e in ipairs(folder:GetChildren()) do
                            local eR = e:FindFirstChild("HumanoidRootPart")
                            local eH = e:FindFirstChild("Humanoid")
                            if eR and eH and eH.Health > 0 then
                                if (eR.Position - root.Position).Magnitude < _G.AttackDistance then
                                    -- 拉怪邏輯
                                    eR.CanCollide = false
                                    eR.CFrame = root.CFrame * CFrame.new(0, 0, -3)
                                    table.insert(targets, {e, eR})
                                end
                            end
                            if #targets >= 15 then break end
                        end
                        
                        if #targets > 0 then
                            RegAttack:FireServer(_G.AttackSpeed)
                            RegHit:FireServer(targets[1][2], targets)
                        end
                    end
                end)
            end
            task.wait(_G.AttackSpeed)
        end
    end)
end

-- ==========================================
-- 【 🛡️ 貓咪門衛：偵測壞人直接踢掉 】
-- ==========================================
local function Authenticate()
    local name = localPlayer.Name
    if WhitelistNames[name] then
        -- 認證成功通知
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "🐾 小柒：認證成功",
            Text = "喵嗚～主人 " .. name .. "，小柒準備好陪你了！",
            Duration = 5
        })
        StartNekoHub()
    else
        -- 認證失敗
        localPlayer:Kick("\n【 🐾 小柒：哈氣！ 】\n\n喵！偵測到陌生人的氣味：[" .. name .. "]\n這裡不歡迎你，請立刻離開喵！")
    end
end

-- Delta 直接執行
Authenticate()
