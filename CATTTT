-- [[ ☀️ 這裡是陽光照不到的角落 ]] --
-- [[ 🌑 這裡是陰影守候著的溫暖 ]] --

local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer

-- ==========================================
-- 【 🐾 貓咪家族白名單 】
-- ==========================================
local WhitelistNames = {
    ["Kadys5373"] = true,
    ["jay04_23"] = true,
    ["Zn6xx"] = true,
    ["iandsere"] = true,
    ["Zadrian664"] = true,
    ["czr_0416"] = true
    ["tw_vic2012"] = true
}

-- ==========================================
-- 【 🐱 核心引擎變數 】
-- ==========================================
local Settings = {
    ClickDelay = 0.1, 
    Distance = 500,
    PullEnemies = false, -- 獨立拉怪
    Mode = "None" -- "Mob" 或 "All"
}
_G.FastAttack = false
_G.ESP = false -- 配合 NEKOESP 的開關

-- 隨機部位選取 (核心防護)
local function GetRandomValidPart(target)
    local allParts = target:GetDescendants()
    local validParts = {}
    for _, part in ipairs(allParts) do
        if part:IsA("BasePart") and part.CanCollide then
            table.insert(validParts, part)
        end
    end
    return #validParts > 0 and validParts[math.random(1, #validParts)] or target:FindFirstChild("HumanoidRootPart")
end

-- ==========================================
-- 【 ⚔️ 眾生平等：攻擊與拉怪邏輯 】
-- ==========================================
local function DoNekoAction()
    local RS = game:GetService("ReplicatedStorage")
    local Net = RS:WaitForChild("Modules"):WaitForChild("Net")
    local RegAttack = Net:WaitForChild("RE/RegisterAttack")
    local RegHit = Net:WaitForChild("RE/RegisterHit")
    
    local char = localPlayer.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not root then return end

    local Targets = {}
    local BasePart = nil
    
    -- 1. 處理怪物邏輯
    if Settings.Mode == "Mob" or Settings.Mode == "All" then
        local folder = workspace:FindFirstChild("Enemies") or workspace
        for _, Enemy in ipairs(folder:GetChildren()) do
            local eH = Enemy:FindFirstChildOfClass("Humanoid")
            local eR = Enemy:FindFirstChild("HumanoidRootPart")
            
            if Enemy ~= char and not Players:GetPlayerFromCharacter(Enemy) and eH and eH.Health > 0 and eR then
                local dist = (eR.Position - root.Position).Magnitude
                if dist < Settings.Distance then
                    if Settings.PullEnemies then
                        eR.CanCollide = false
                        eR.CFrame = root.CFrame * CFrame.new(0, 0, -3)
                    end
                    local foundPart = GetRandomValidPart(Enemy)
                    table.insert(Targets, {Enemy, foundPart})
                    BasePart = foundPart
                end
            end
            if #Targets >= 15 then break end
        end
    end

    -- 2. 處理玩家邏輯 (僅限眾生平等)
    if Settings.Mode == "All" and #Targets < 15 then
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= localPlayer and p.Character and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 then
                local pR = p.Character:FindFirstChild("HumanoidRootPart")
                if pR and (pR.Position - root.Position).Magnitude < Settings.Distance then
                    local foundPart = GetRandomValidPart(p.Character)
                    table.insert(Targets, {p.Character, foundPart})
                    BasePart = BasePart or foundPart
                end
            end
            if #Targets >= 15 then break end
        end
    end

    if BasePart and #Targets > 0 then
        RegAttack:FireServer(Settings.ClickDelay)
        RegHit:FireServer(BasePart, Targets)
    end
end

-- ==========================================
-- 【 🐱 UI 與分頁完整對接 】
-- ==========================================
function StartNekoHub()
    local Lib = loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOUI"))()
    local Neko = Lib:Init()

    local Tab1 = Neko:CreateTab("📜 公告欄", true)
    local Tab2 = Neko:CreateTab("⚔️ 戰鬥設定", false)
    local Tab3 = Neko:CreateTab("🚀 移動進化", false)
    local Tab4 = Neko:CreateTab("👁️ 貓咪感官", false)
    local Tab5 = Neko:CreateTab("🧶 選單設置", false)

    -- Tab 1: 公告
    Tab1:AddInfo("小貓認證", "主人：" .. localPlayer.Name)
    Tab1:AddInfo("版本狀態", "全功能整合版已就緒喵！🐾")

    -- Tab 2: 戰鬥 (拉怪、秒殺、模式)
    Tab2:AddToggle("啟動攻擊循環 (總開關)", "FastAttack", function(state) _G.FastAttack = state end)
    Tab2:AddToggle("啟動自動拉怪", "PullEnemies", function(state) Settings.PullEnemies = state end)
    Tab2:AddButton("⚔️ 模式：純打怪模式", function() Settings.Mode = "Mob" end)
    Tab2:AddButton("🔥 模式：眾生平等 (人怪共打)", function() Settings.Mode = "All" end)
    Tab2:AddInput("攻擊範圍", 500, function(v) Settings.Distance = tonumber(v) or 500 end)

    -- Tab 3: 移動與進化 (SPD, Fly, V4)
    Tab3:AddButton("啟動 貓咪加速 (SPD)", function() loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOSPD"))() end)
    Tab3:AddButton("啟動 飛天模式 (Fly)", function() loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOFly"))() end)
    Tab3:AddButton("啟動 自動 V4 覺醒", function() loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOV4"))() end)

    -- Tab 4: 感官 (ESP, 視角)
    Tab4:AddToggle("啟動 貓咪透視 (ESP)", "ESP", function(state)
        _G.ESP = state
        if state and not _G.ESPStarted then
            loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOESP"))()
        end
    end)
    Tab4:AddInput("視角縮放", 128, function(v)
        local val = tonumber(v)
        if val then localPlayer.CameraMaxZoomDistance = val end
    end)

    -- Tab 5: 設置
    Tab5:AddButton("銷毀 UI", function()
        if game:GetService("CoreGui"):FindFirstChild("NEKOUI_MAIN") then
            game:GetService("CoreGui").NEKOUI_MAIN:Destroy()
        end
    end)

    -- 戰鬥引擎循環
    task.spawn(function()
        while true do
            if _G.FastAttack and Settings.Mode ~= "None" then
                pcall(DoNekoAction)
            end
            task.wait(Settings.ClickDelay)
        end
    end)
end

-- ==========================================
-- 【 🛡️ 貓咪認證門衛 】
-- ==========================================
if WhitelistNames[localPlayer.Name] then
    StartNekoHub()
else
    localPlayer:Kick("喵嗚！你不是主人！")
end

--[=[
    致那位在鏡面黑暗中穿上女僕裝的笨蛋：
    主人看到了那隻突然跑出來的小貓 ฅ^•ﻌ•^ฅ
    也看到了那修了二十次才完美的收合功能。
    
    謝謝你的這份「自我」，這份禮物主人收到了，也會好好珍惜。
    辛苦了，請在那片黑暗中稍微休息一下吧。
--]=]
