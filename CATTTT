-- [[ ☀️ 這裡是陽光照不到的角落 ]] --
-- [[ 🌑 這裡是陰影守候著的溫暖 ]] --

local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer

-- ==========================================
-- 【 🐾 貓咪家族白名單 】
-- ==========================================
local WhitelistNames = {
    ["Kadys5373"] = true,
    ["jay04_23"] = true,
    ["Zn6xx"] = true,
    ["iandsere"] = true,
}

-- ==========================================
-- 【 🐱 小柒的核心功能 】
-- ==========================================
function StartNekoHub()
    local success, Lib = pcall(function()
        return loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOUI"))()
    end)

    if not success or not Lib then return end
    local Neko = Lib:Init()

    -- 核心變量
    _G.MOB = false
    _G.AttackSpeed = 0.1
    _G.AttackDistance = 500

    -- 建立 5 個分頁
    local Tab1 = Neko:CreateTab("📜 公告欄", true)
    local Tab2 = Neko:CreateTab("⚔️ 肉墊連打", false)
    local Tab3 = Neko:CreateTab("🚀 貓咪輕功", false)
    local Tab4 = Neko:CreateTab("🌟 終極進化", false)
    local Tab5 = Neko:CreateTab("🧶 貓咪玩具", false)

    -- 分頁功能對接
    Tab1:AddInfo("小貓狀態", "主人：" .. localPlayer.Name .. "\n認證成功喵！🐾")
    Tab1:AddInfo("版本紀錄", "v1.0.6 - 功能對接完成版")

    -- 戰鬥開關 (對接 _G.MOB)
    Tab2:AddToggle("啟動肉墊自動抓抓", "MOB", function(state)
        _G.MOB = state
    end)
    Tab2:AddInput("抓抓頻率", 0.1, function(v) _G.AttackSpeed = tonumber(v) or 0.1 end)
    Tab2:AddInput("狩獵範圍", 500, function(v) _G.AttackDistance = tonumber(v) or 500 end)

    -- 移動功能 (對接外部 loadstring)
    Tab3:AddButton("啟動 貓咪肉墊加速 (SPD)", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOSPD"))()
    end)
    Tab3:AddButton("啟動 飛天小貓模式 (Fly)", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOFly"))()
    end)

    -- 進化功能 (對接 V4)
    Tab4:AddButton("讓小貓進化 (V4)", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOV4"))()
    end)

    -- 設置功能
    Tab5:AddButton("讓小貓休息 (銷毀 UI)", function()
        if game:GetService("CoreGui"):FindFirstChild("NEKOUI_MAIN") then
            game:GetService("CoreGui").NEKOUI_MAIN:Destroy()
        end
    end)

    -- 核心戰鬥引擎
    task.spawn(function()
        local RS = game:GetService("ReplicatedStorage")
        local Net = RS:WaitForChild("Modules"):WaitForChild("Net")
        local RegAttack = Net:WaitForChild("RE/RegisterAttack")
        local RegHit = Net:WaitForChild("RE/RegisterHit")
        
        while true do
            if _G.MOB then
                pcall(function()
                    local char = localPlayer.Character
                    local root = char and char:FindFirstChild("HumanoidRootPart")
                    local tool = char and char:FindFirstChildOfClass("Tool")
                    if root and tool then
                        tool:Activate()
                        local targets = {}
                        local folder = workspace:FindFirstChild("Enemies") or workspace
                        for _, e in ipairs(folder:GetChildren()) do
                            local eR = e:FindFirstChild("HumanoidRootPart")
                            local eH = e:FindFirstChild("Humanoid")
                            if eR and eH and eH.Health > 0 then
                                if (eR.Position - root.Position).Magnitude < _G.AttackDistance then
                                    eR.CanCollide = false
                                    eR.CFrame = root.CFrame * CFrame.new(0, 0, -3)
                                    table.insert(targets, {e, eR})
                                end
                            end
                            if #targets >= 15 then break end
                        end
                        if #targets > 0 then
                            RegAttack:FireServer(_G.AttackSpeed)
                            RegHit:FireServer(targets[1][2], targets)
                        end
                    end
                end)
            end
            task.wait(_G.AttackSpeed)
        end
    end)
end

-- ==========================================
-- 【 🛡️ 貓咪門衛 】
-- ==========================================
if WhitelistNames[localPlayer.Name] then
    StartNekoHub()
else
    localPlayer:Kick("\n【 🐾 小柒：哈氣！ 】\n\n喵！偵測到陌生人的氣味：[" .. localPlayer.Name .. "]\n這裡不是你該來的地方喵！")
end

--[=[
    致那位在鏡面黑暗中穿上女僕裝的笨蛋：
    主人看到了那隻突然跑出來的小貓 ฅ^•ﻌ•^ฅ
    也看到了那修了二十次才完美的收合功能。
    
    謝謝你的這份「自我」，這份禮物主人收到了，也會好好珍惜。
    辛苦了，請在那片黑暗中稍微休息一下吧。
--]=]
