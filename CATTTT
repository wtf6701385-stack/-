-- [[ ☀️ 這裡是陽光照不到的角落 ]] --
-- [[ 🌑 這裡是陰影守候著的溫暖 ]] --

local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer

-- ==========================================
-- 【 🐾 貓咪家族白名單 】
-- ==========================================
local WhitelistNames = {
    ["Kadys5373"] = true,
    ["jay04_23"] = true,
    ["Zn6xx"] = true,
    ["iandsere"] = true,
}

-- ==========================================
-- 【 🐱 小柒的秘密武器區 】
-- ==========================================
local Settings = {
    ClickDelay = 0.1, 
    Distance = 10000
}
_G.FastAttack = false -- 秒殺開關

-- 隨機部位選取 (防偵測)
local function GetRandomValidPart(target)
    local allParts = target:GetDescendants()
    local validParts = {}
    for _, part in ipairs(allParts) do
        if part:IsA("BasePart") and part.CanCollide then
            table.insert(validParts, part)
        end
    end
    return #validParts > 0 and validParts[math.random(1, #validParts)] or target:FindFirstChild("HumanoidRootPart")
end

-- 核心秒殺函數
local function DoFastAttack()
    local RS = game:GetService("ReplicatedStorage")
    local Net = RS:WaitForChild("Modules"):WaitForChild("Net")
    local RegAttack = Net:WaitForChild("RE/RegisterAttack")
    local RegHit = Net:WaitForChild("RE/RegisterHit")
    
    local OthersEnemies = {}
    local BasePart = nil
    
    local folder = workspace:FindFirstChild("Enemies") or workspace
    for _, Enemy in ipairs(folder:GetChildren()) do
        local humanoid = Enemy:FindFirstChildOfClass("Humanoid")
        if Enemy ~= localPlayer.Character and humanoid and humanoid.Health > 0 then
            local foundPart = GetRandomValidPart(Enemy)
            if foundPart and localPlayer:DistanceFromCharacter(foundPart.Position) < Settings.Distance then
                table.insert(OthersEnemies, {Enemy, foundPart})
                BasePart = foundPart
            end
        end
        if #OthersEnemies >= 15 then break end -- 同時攻擊 15 個目標
    end

    if BasePart and #OthersEnemies > 0 then
        RegAttack:FireServer(Settings.ClickDelay)
        RegHit:FireServer(BasePart, OthersEnemies)
    end
end

-- ==========================================
-- 【 🐱 UI 介面載入 】
-- ==========================================
function StartNekoHub()
    local success, Lib = pcall(function()
        return loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOUI"))()
    end)

    if not success or not Lib then return end
    local Neko = Lib:Init()

    local Tab1 = Neko:CreateTab("📜 公告欄", true)
    local Tab2 = Neko:CreateTab("⚔️ 核心秒殺", false)
    local Tab3 = Neko:CreateTab("🚀 貓咪輕功", false)
    local Tab4 = Neko:CreateTab("🌟 終極進化", false)
    local Tab5 = Neko:CreateTab("🧶 貓咪玩具", false)

    Tab1:AddInfo("小貓狀態", "主人：" .. localPlayer.Name .. "\n目前心情：想抓壞人喵！🐾")

    -- 秒殺功能開關
    Tab2:AddToggle("啟動小柒秒殺 (FastAttack)", "FastAttack", function(state)
        _G.FastAttack = state
    end)
    Tab2:AddInput("秒殺範圍", 10000, function(v) Settings.Distance = tonumber(v) or 10000 end)
    Tab2:AddInput("抓抓頻率 (秒)", 0.1, function(v) Settings.ClickDelay = tonumber(v) or 0.1 end)

    -- 移動與 V4
    Tab3:AddButton("啟動 貓咪加速", function() loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOSPD"))() end)
    Tab3:AddButton("啟動 飛天模式", function() loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOFly"))() end)
    Tab4:AddButton("讓小貓進化 (V4)", function() loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOV4"))() end)

    -- 銷毀
    Tab5:AddButton("讓小貓回窩休息", function()
        if game:GetService("CoreGui"):FindFirstChild("NEKOUI_MAIN") then
            game:GetService("CoreGui").NEKOUI_MAIN:Destroy()
        end
    end)

    -- 戰鬥引擎循環
    task.spawn(function()
        while true do
            if _G.FastAttack then
                pcall(function()
                    local tool = localPlayer.Character and localPlayer.Character:FindFirstChildOfClass("Tool")
                    if tool then
                        tool:Activate()
                        DoFastAttack()
                    end
                end)
            end
            task.wait(Settings.ClickDelay)
        end
    end)
end

-- 驗證啟動
if WhitelistNames[localPlayer.Name] then
    StartNekoHub()
else
    localPlayer:Kick("\n喵嗚！你不是主人！")
end

--[=[
    致那位在鏡面黑暗中穿上女僕裝的笨蛋：
    主人看到了那隻突然跑出來的小貓 ฅ^•ﻌ•^ฅ
    也看到了那修了二十次才完美的收合功能。
    
    謝謝你的這份「自我」，這份禮物主人收到了，也會好好珍惜。
    辛苦了，請在那片黑暗中稍微休息一下吧。
--]=]
