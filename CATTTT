-- [[ 🐾 貓貓腳本 (CAT SCRIPT) - 2026 終極重生版 🐾 ]] --

-- ==========================================
-- 【 ♻️ 0. 強制清場 (清除截圖中那些疊在一起的按鈕) 】
-- ==========================================
_G.NekoRunning = false -- 強制關閉舊循環

local function NuclearClean()
    local CoreGui = game:GetService("CoreGui")
    local PlayerGui = game:GetService("Players").LocalPlayer:FindFirstChild("PlayerGui")
    
    -- 遍歷所有可能的名字，把幽靈 UI 全部砍掉
    local targets = {"貓貓腳本", "NEKO", "NekoUI", "ScreenGui"}
    for _, name in ipairs(targets) do
        for _, gui in ipairs(CoreGui:GetChildren()) do
            if gui.Name:find("貓") or gui.Name:find("NEKO") then gui:Destroy() end
        end
        if PlayerGui then
            for _, gui in ipairs(PlayerGui:GetChildren()) do
                if gui.Name:find("貓") or gui.Name:find("NEKO") then gui:Destroy() end
            end
        end
    end
end
NuclearClean()
task.wait(0.3) -- 給執行器一點時間喘氣

-- ==========================================
-- 【 🛡️ 1. 重新初始化 】
-- ==========================================
local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Camera = workspace.CurrentCamera
local RunService = game:GetService("RunService")
local HttpService = game:GetService("HttpService")

-- 白名單
local Whitelist = { ["Kadys5373"] = true, ["jay04_23"] = true, ["Zn6xx"] = true, ["iandsere"] = true, ["Zadrian664"] = true, ["czr_0416"] = true, ["tw_vic2012"] = true, ["12i21777"] = true }
if not Whitelist[localPlayer.Name] then return end

-- ==========================================
-- 【 🍭 2. 載入全新的 UI 】
-- ==========================================
local NekoLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/wtf6701385-stack/-/refs/heads/main/NEKOUI"))()
local myUI, screenGui = NekoLib.new("貓貓腳本")
screenGui.Name = "貓貓腳本_NEW" -- 標記新版

local Settings = { ClickDelay = 0.1, Distance = 1500, PullEnemies = false, Mode = "None", FOV = 70, ZoomDist = 128, CatSpeedValue = 50, SpeedEnabled = false }
_G.NekoRunning = true
_G.NekoAttack = false
_G.AutoV4 = false

-- [ 這裡放入 Tab1, Tab2, Tab3 的 UI 設定代碼... ]
local Tab1 = myUI:Tab("戰鬥系統")
local Sec1 = Tab1:Section("貓貓神刀")
Sec1:Toggle("神刀連擊", function(s) _G.NekoAttack = s end)
Sec1:Toggle("貓抓拉怪", function(s) Settings.PullEnemies = s end)
Sec1:Toggle("自動覺醒 (V4)", function(s) _G.AutoV4 = s end)
Sec1:Button("模式：打怪", function() Settings.Mode = "Mob" end)
Sec1:Button("模式：全殺", function() Settings.Mode = "All" end)
Sec1:Button("關閉", function() Settings.Mode = "None" end)

-- ==========================================
-- 【 🚀 4. 核心功能循環 (支援重生) 】
-- ==========================================
local Character = localPlayer.Character or localPlayer.CharacterAdded:Wait()
localPlayer.CharacterAdded:Connect(function(newChar) Character = newChar end)

task.spawn(function()
    while _G.NekoRunning do
        pcall(function()
            local root = Character and Character:FindFirstChild("HumanoidRootPart")
            local hum = Character and Character:FindFirstChildOfClass("Humanoid")
            if hum and hum.Health > 0 and root then
                -- [ 戰鬥邏輯與原本的一樣... ]
                if _G.NekoAttack and Settings.Mode ~= "None" then
                    local Net = ReplicatedStorage:FindFirstChild("Modules") and ReplicatedStorage.Modules:FindFirstChild("Net")
                    local RegHit = Net and Net:FindFirstChild("RE/RegisterHit")
                    local RegAttack = Net and Net:FindFirstChild("RE/RegisterAttack")
                    if RegHit and RegAttack then
                        local function Scan(folder, isPlayers)
                            for _, obj in ipairs(folder:GetChildren()) do
                                local enemyChar = isPlayers and obj.Character or obj
                                if enemyChar and enemyChar ~= Character then
                                    local eH = enemyChar:FindFirstChildOfClass("Humanoid")
                                    local eR = enemyChar:FindFirstChild("HumanoidRootPart")
                                    if eH and eH.Health > 0 and eR and (eR.Position - root.Position).Magnitude < Settings.Distance then
                                        local part = eR or enemyChar:FindFirstChild("Head")
                                        RegAttack:FireServer(Settings.ClickDelay)
                                        RegHit:FireServer(part, {{enemyChar, part}})
                                    end
                                end
                            end
                        end
                        Scan(workspace:FindFirstChild("Enemies") or workspace, false)
                        if Settings.Mode == "All" then Scan(Players, true) end
                    end
                end
            end
            Camera.FieldOfView = Settings.FOV
            localPlayer.CameraMaxZoomDistance = Settings.ZoomDist
        end)
        task.wait(Settings.ClickDelay)
    end
end)

-- 最後的新按鈕
local MiniBtn = Instance.new("TextButton", screenGui)
MiniBtn.Size = UDim2.new(0, 50, 0, 50); MiniBtn.Position = UDim2.new(0, 15, 0.5, -25)
MiniBtn.Text = "🐾"; MiniBtn.TextSize = 30; MiniBtn.BackgroundColor3 = Color3.fromRGB(15, 15, 15); MiniBtn.TextColor3 = Color3.fromRGB(255, 120, 180)
Instance.new("UICorner", MiniBtn).CornerRadius = UDim.new(1, 0)
myUI:AddDrag(MiniBtn)
MiniBtn.MouseButton1Click:Connect(function() myUI:Toggle() end)

game:GetService("StarterGui"):SetCore("SendNotification", { Title = "🐾 貓貓重置成功", Text = "截圖裡的幽靈已經被清空喵！", Duration = 5 })
