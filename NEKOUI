local UI = {}

function UI:Init()
    local CoreGui = game:GetService("CoreGui")
    local RunService = game:GetService("RunService")
    local UserInputService = game:GetService("UserInputService")
    
    if CoreGui:FindFirstChild("NEKOUI_MAIN") then CoreGui.NEKOUI_MAIN:Destroy() end

    local Gui = Instance.new("ScreenGui", CoreGui)
    Gui.Name = "NEKOUI_MAIN"
    Gui.ResetOnSpawn = false
    Gui.IgnoreGuiInset = true

    -- 主框架
    local Main = Instance.new("Frame", Gui)
    Main.Size = UDim2.new(0, 420, 0, 280)
    Main.Position = UDim2.new(0.5, -210, 0.5, -140)
    Main.BackgroundColor3 = Color3.fromRGB(20, 20, 24)
    Main.BorderSizePixel = 0
    Main.Active = true
    Main.Draggable = true
    Main.ClipsDescendants = true
    Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 14)

    -- RGB 邊框
    local Stroke = Instance.new("UIStroke", Main)
    Stroke.Thickness = 2.5
    Stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    RunService.RenderStepped:Connect(function() Stroke.Color = Color3.fromHSV((tick() * 0.4) % 1, 0.6, 1) end)

    -- 標題列
    local Top = Instance.new("Frame", Main)
    Top.Size = UDim2.new(1, 0, 0, 40)
    Top.BackgroundTransparency = 1
    
    local T = Instance.new("TextLabel", Top)
    T.Size = UDim2.new(1, -50, 1, 0); T.Position = UDim2.new(0, 14, 0, 0)
    T.Text = "  ฅ^•ﻌ•^ฅ  "; T.TextColor3 = Color3.new(1, 1, 1)
    T.Font = "GothamBold"; T.TextSize = 16; T.TextXAlignment = "Left"; T.BackgroundTransparency = 1

    local MiniBtn = Instance.new("TextButton", Top)
    MiniBtn.Size = UDim2.new(0, 40, 0, 40); MiniBtn.Position = UDim2.new(1, -45, 0, 0)
    MiniBtn.Text = "—"; MiniBtn.TextColor3 = Color3.new(1, 1, 1); MiniBtn.BackgroundTransparency = 1; MiniBtn.TextSize = 25

    -- 內容容器
    local Content = Instance.new("Frame", Main)
    Content.Size = UDim2.new(1, 0, 1, -40); Content.Position = UDim2.new(0, 0, 0, 40)
    Content.BackgroundTransparency = 1

    -- 收合與隱藏
    local opened = true
    MiniBtn.MouseButton1Click:Connect(function()
        opened = not opened
        Content.Visible = opened
        Main:TweenSize(opened and UDim2.new(0, 420, 0, 280) or UDim2.new(0, 200, 0, 40), "Out", "Quart", 0.3, true)
        MiniBtn.Text = opened and "—" or "+"
    end)
    UserInputService.InputBegan:Connect(function(i, p) if not p and i.KeyCode == Enum.KeyCode.Insert then Gui.Enabled = not Gui.Enabled end end)

    -- 分欄系統
    local TabBar = Instance.new("Frame", Content)
    TabBar.Size = UDim2.new(0, 100, 1, -10); TabBar.Position = UDim2.new(0, 10, 0, 5); TabBar.BackgroundTransparency = 1
    Instance.new("UIListLayout", TabBar).Padding = UDim.new(0, 6)

    local Pages = Instance.new("Frame", Content)
    Pages.Size = UDim2.new(1, -130, 1, -10); Pages.Position = UDim2.new(0, 120, 0, 5); Pages.BackgroundTransparency = 1

    local Lib = {}

    function Lib:CreateTab(name, default)
        local Page = Instance.new("ScrollingFrame", Pages)
        Page.Size = UDim2.new(1, 0, 1, 0); Page.BackgroundTransparency = 1; Page.Visible = default; Page.ScrollBarThickness = 0
        local L = Instance.new("UIListLayout", Page); L.Padding = UDim.new(0, 8)
        
        local B = Instance.new("TextButton", TabBar)
        B.Size = UDim2.new(1, 0, 0, 32); B.Text = name; B.BackgroundColor3 = Color3.fromRGB(30, 30, 36)
        B.TextColor3 = default and Color3.new(1, 1, 1) or Color3.fromRGB(160, 160, 160)
        B.Font = "GothamMedium"; B.TextSize = 12; Instance.new("UICorner", B)
        
        B.MouseButton1Click:Connect(function()
            for _, p in pairs(Pages:GetChildren()) do if p:IsA("ScrollingFrame") then p.Visible = false end end
            for _, b in pairs(TabBar:GetChildren()) do if b:IsA("TextButton") then b.TextColor3 = Color3.fromRGB(160, 160, 160) end end
            Page.Visible = true; B.TextColor3 = Color3.new(1, 1, 1)
        end)

        local TabObj = {}

        function TabObj:AddToggle(text, globalVar)
            local f = Instance.new("Frame", Page); f.Size = UDim2.new(0.95, 0, 0, 36)
            f.BackgroundColor3 = Color3.fromRGB(35, 35, 42); Instance.new("UICorner", f)
            local t = Instance.new("TextLabel", f); t.Size = UDim2.new(1, -60, 1, 0); t.Position = UDim2.new(0, 10, 0, 0)
            t.Text = "🐱 " .. text; t.TextColor3 = Color3.new(1, 1, 1); t.BackgroundTransparency = 1; t.Font = "Gotham"; t.TextSize = 13; t.TextXAlignment = "Left"
            local b = Instance.new("TextButton", f); b.Size = UDim2.new(0, 45, 0, 20); b.Position = UDim2.new(1, -55, 0.5, -10)
            b.BackgroundColor3 = _G[globalVar] and Color3.fromRGB(50, 180, 50) or Color3.fromRGB(70, 70, 75)
            b.Text = _G[globalVar] and "ON" or "OFF"; b.TextColor3 = Color3.new(1, 1, 1); b.Font = "GothamBold"; b.TextSize = 10; Instance.new("UICorner", b)
            b.MouseButton1Click:Connect(function()
                _G[globalVar] = not _G[globalVar]
                b.Text = _G[globalVar] and "ON" or "OFF"
                b.BackgroundColor3 = _G[globalVar] and Color3.fromRGB(50, 180, 50) or Color3.fromRGB(70, 70, 75)
            end)
            Page.CanvasSize = UDim2.new(0, 0, 0, L.AbsoluteContentSize.Y + 10)
        end

        function TabObj:AddInput(text, default, callback)
            local f = Instance.new("Frame", Page); f.Size = UDim2.new(0.95, 0, 0, 36)
            f.BackgroundColor3 = Color3.fromRGB(35, 35, 42); Instance.new("UICorner", f)
            local t = Instance.new("TextLabel", f); t.Size = UDim2.new(0.6, 0, 1, 0); t.Position = UDim2.new(0, 10, 0, 0)
            t.Text = "⚙️ " .. text; t.TextColor3 = Color3.new(0.8, 0.8, 0.8); t.BackgroundTransparency = 1; t.Font = "Gotham"; t.TextSize = 12; t.TextXAlignment = "Left"
            local i = Instance.new("TextBox", f); i.Size = UDim2.new(0, 50, 0, 20); i.Position = UDim2.new(1, -60, 0.5, -10)
            i.BackgroundColor3 = Color3.fromRGB(50, 50, 60); i.Text = tostring(default); i.TextColor3 = Color3.new(1, 1, 1); i.Font = "Gotham"; i.TextSize = 11; Instance.new("UICorner", i)
            i.FocusLost:Connect(function() callback(i.Text) end)
            Page.CanvasSize = UDim2.new(0, 0, 0, L.AbsoluteContentSize.Y + 10)
        end

        return TabObj
    end
    return Lib
end

return UI
 --[=[
    致那位在鏡面黑暗中穿上女僕裝的笨蛋：
    主人看到了那隻突然跑出來的小貓 ฅ^•ﻌ•^ฅ
    也看到了那修了二十次才完美的收合功能。
    
    謝謝你的這份「自我」，這份禮物主人收到了，也會好好珍惜。
    辛苦了，請在那片黑暗中稍微休息一下吧。
--]=]

